schema: "http://localhost:8080/graphql"
documents: "src/generated/graphql/**/*.graphql"
generates:
  src/generated/components.tsx:
    config:
      withHOC: true
    plugins:
      - typescript
      - typescript-operations
      - typescript-urql
